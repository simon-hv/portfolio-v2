---
import { cn } from "@/lib/utils";

export interface Props {
  text: string;
  classNames?: string;
}

const { text, classNames } = Astro.props;
---

<span
  class={cn(
    "bg-clip-text text-transparent bg-gradient-to-r from-red-200 to-yellow-200",
    classNames,
  )}>{text}</span
>
